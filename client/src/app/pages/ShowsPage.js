import { ShowsList } from '../components/project/Shows';
import { BaseLayout } from '../layouts';

const ProjectsPage = () => {
  return (
    <BaseLayout>
      <ShowsList />
    </BaseLayout>
  );
};

export default ProjectsPage;